/**
 * Configuration settings for the aggregator server
 */

// Server port
export const PORT = process.env.PORT || 3001;

// WebSocket configuration
export const WS_CONFIG = {
  // Add any WebSocket specific configuration here
};
